export declare function format(first: string, middle: string, last: string): string;
export declare class StorageSaqhanPromo {
    private static prefix_;
    static get prefix(): string;
    static set prefix(val: string);
}
